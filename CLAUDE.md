@AGENTS.md

## Claude Code 전용

- 진행 상태는 볼트 `~/dev/data/vault/projects/api-server/현황.md` 에 둔다 — 세션 끝에 `/handoff`. `AGENTS.md` 의 "현재 남은 작업" 절은 첫 `/handoff` 때 볼트로 옮기고 거기서 지운다.
- `main` 푸시 = 프로덕션 배포. `dev → main` 은 사용자 승인 없이 하지 않는다(전역 working-agreement 6절).
- 커밋 형식·AI 표기 금지는 전역 `~/.claude/rules/git-workflow.md`.
